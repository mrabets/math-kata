<div class="container">
  <% if current_user %>
    <h3><%= @user.name unless current_user.name.nil? %></h3>
    <h3><%= @user.email %></h3>
    <%= image_tag(@user.image) unless current_user.image.nil? %>
  <% end %>
</div>
